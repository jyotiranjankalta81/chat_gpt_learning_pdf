import { z } from 'zod';
export const listJobsSchema = z.object({ query: z.object({ page: z.coerce.number().int().positive().default(1), limit: z.coerce.number().int().positive().max(100).default(25), search: z.string().trim().optional(), companyId: z.string().trim().optional(), location: z.string().trim().optional(), skill: z.string().trim().optional(), experienceMin: z.coerce.number().int().min(0).optional(), experienceMax: z.coerce.number().int().min(0).optional(), postedFrom: z.coerce.date().optional(), postedTo: z.coerce.date().optional(), sortBy: z.enum(['postedDate', 'createdAt', 'title', 'location', 'experienceMin']).default('postedDate'), sortOrder: z.enum(['asc', 'desc']).default('desc') }) });
export const idParamSchema = z.object({ params: z.object({ id: z.string().min(1) }) });
export const syncSchema = z.object({ body: z.object({ force: z.boolean().optional() }).default({}) });
