import type { ProviderJobInput } from '../../types.js';
export function sampleJob(companyName: string, suffix: string, location: string): ProviderJobInput { return { companyName, jobId: `${companyName.toLowerCase().replace(/[^a-z0-9]+/g, '-')}-${suffix}`, title: suffix.includes('senior') ? 'Senior Java Developer' : 'Java Backend Developer', location, employmentType: 'Full-time', experienceMin: suffix.includes('senior') ? 4 : 2, experienceMax: 5, skills: ['Java', 'Spring Boot', 'Microservices', 'Kafka', 'Redis', 'AWS', 'Docker', 'Kubernetes'], salary: 'Competitive', postedDate: new Date(), applyUrl: `https://careers.example.com/${companyName.toLowerCase().replace(/[^a-z0-9]+/g, '-')}/${suffix}`, jobDescription: 'Build scalable Java services using Spring Boot, microservices, Kafka, Redis, SQL databases, Docker, Kubernetes, and cloud platforms for India engineering teams.', source: `${companyName} Careers` }; }
