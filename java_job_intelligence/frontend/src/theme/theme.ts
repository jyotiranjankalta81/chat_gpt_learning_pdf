import { createTheme } from '@mui/material/styles';
export const theme = createTheme({ palette: { mode: 'light', primary: { main: '#1f4e79' }, secondary: { main: '#0f9d58' }, background: { default: '#f6f8fb' } }, typography: { fontFamily: ['Inter', 'Roboto', 'Arial', 'sans-serif'].join(',') }, shape: { borderRadius: 12 } });
