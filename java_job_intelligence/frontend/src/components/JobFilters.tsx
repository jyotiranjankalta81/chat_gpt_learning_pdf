import { Button, MenuItem, Stack, TextField } from '@mui/material';
import type { Company, JobFilters as Filters } from '../types/job';
interface Props { companies: Company[]; filters: Filters; onChange: (filters: Filters) => void; onExport: () => void; onSync: () => void; }
export function JobFilters({ companies, filters, onChange, onExport, onSync }: Props) { const update = (key: keyof Filters, value: string) => onChange({ ...filters, [key]: value || undefined, page: 1 }); return <Stack direction={{ xs: 'column', md: 'row' }} spacing={2} alignItems="center"><TextField label="Search" size="small" value={filters.search ?? ''} onChange={(event) => update('search', event.target.value)} fullWidth /><TextField label="Company" size="small" select value={filters.companyId ?? ''} onChange={(event) => update('companyId', event.target.value)} sx={{ minWidth: 180 }}><MenuItem value="">All</MenuItem>{companies.map((company) => <MenuItem key={company._id} value={company._id}>{company.name}</MenuItem>)}</TextField><TextField label="Location" size="small" value={filters.location ?? ''} onChange={(event) => update('location', event.target.value)} /><TextField label="Skill" size="small" value={filters.skill ?? ''} onChange={(event) => update('skill', event.target.value)} /><TextField label="Min Exp" size="small" type="number" value={filters.experienceMin ?? ''} onChange={(event) => update('experienceMin', event.target.value)} sx={{ width: 110 }} /><TextField label="Max Exp" size="small" type="number" value={filters.experienceMax ?? ''} onChange={(event) => update('experienceMax', event.target.value)} sx={{ width: 110 }} /><Button variant="outlined" onClick={onSync}>Sync</Button><Button variant="contained" onClick={onExport}>Export</Button></Stack>; }
